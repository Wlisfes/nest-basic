export interface OSSAuthorize {
    accessKeyId: string
    accessKeySecret: string
    endpoint: string
    bucket: string
    region: string
    token: string
    expire: string
    interval: number
}
